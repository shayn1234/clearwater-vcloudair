$TTL 1h
@ IN SOA ns1.example.com. shay.gigaspaces.com. (
                                                        2009010910 ;serial
                                                        3600 ;refresh
                                                        3600 ;retry
                                                        3600 ;expire
                                                        3600 ;minimum TTL
)
@                       IN NS    ns1.example.com.
ns1             3600    IN A     <%= node['cloud']['local_ipv4'].split(',')[0] %>

<%
@records.each do |rec|
-%>
<%= rec['name'] %> IN A <%= rec['node']['cloud']['local_ipv4'].split(',')[0] %> ; <%= rec['node'].name %>
<%
end
-%>

; V2
